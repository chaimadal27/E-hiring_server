# from django.db import models
# from ...legal_agency.models import LegalAgency
# from ...user_managment.models import Profile

# class BusinessUnit(models.Model):
#     # business_unit_name = models.CharField(max_length = 50)
#     # legal_agency = models.ForeignKey(LegalAgency, on_delete=models.CASCADE)
#     # business_unit_manager = models.ForeignKey(Profile, on_delete=models.SET_NULL, null=True)
#     pass